#ifndef SETTINGPTRS
#define SETTINGPTRS

namespace ammonite {
  namespace settings {
    namespace controls {
      namespace internal {
        float* getMovementSpeedPtr();
        float* getMouseSpeedPtr();
        float* getZoomSpeedPtr();
      }
    }
  }
}

#endif
