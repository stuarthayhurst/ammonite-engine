#ifndef EXTENSION
#define EXTENSION

namespace ammonite {
  namespace utils {
    bool checkExtension(const char extension[], const char version[]);
    bool checkExtension(const char extension[]);
  }
}

#endif
